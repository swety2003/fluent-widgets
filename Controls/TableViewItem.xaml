<UserControl x:Class="MyNewApp.Controls.TableViewItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyNewApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Height="36">
            <TextBlock x:Name="tcurrent" Padding="10 5" Foreground="{StaticResource TextFillColorPrimaryBrush}" FontSize="24" FontWeight="Bold" Text="{Binding Current}"/>
            <TextBlock FontSize="12"   Foreground="{StaticResource TextFillColorPrimaryBrush}" FontWeight="Normal" Margin="0 16 0 0">/</TextBlock>
            <TextBlock x:Name="tmax" FontSize="12"   Foreground="{StaticResource TextFillColorPrimaryBrush}" FontWeight="Normal" Margin="0 16 0 0" Text="{Binding Max}"/>

        </StackPanel>
        <TextBlock x:Name="tname" Padding="10 5" Text="{Binding TName}" Foreground="{StaticResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</UserControl>
